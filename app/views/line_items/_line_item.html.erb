<% if line_item == @current_item %>
  <tr id="current_item">
<% else %>
  <tr>
<% end %>
    <td><%= line_item.car.brand %> <%= line_item.car.model %></td>
    <td>Hours: <%= line_item.hours %></td>
    <td class="item_price"><%= number_to_currency(line_item.total_price) %></td>
</tr>